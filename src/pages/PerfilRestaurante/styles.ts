import styled from 'styled-components'
import { cores } from '../../styles'

export const PerfilContainer = styled.div`
  max-width: 1024px;
  width: 100%;
  margin: 0 auto;
  position: relative;
  background-color: ${cores.begeClaro};
  /* padding opcional para afastar conteúdos do topo */
  /* padding-top: 16px; */
`
